Readme file to study git

# Git commands
$ git init
$ git status
$ git log --oneline --decorate --graph --all
$ git branch
$ git checkout -b ""
$ git add
$ git commit -m ""
$ git revert
$ git reset

# Add some more info to readme
just to check
